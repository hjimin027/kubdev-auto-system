version: "3.8"
services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    environment:
      - KUBEDEV_MOCK=1
    ports:
      - "8000:8000"
    restart: unless-stopped

